CREATE TABLE Administrador
(
	Id_usuario INT PRIMARY KEY,
    Fecha_posicionamiento DATE NOT NULL,
    Area_acceso VARCHAR(15) NOT NULL
);

-- Técnicamente estaríamos aplicador TPT : Una 'herencia' aplicada a bases de datos.